{
  "name": "refacto-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "for i in $(seq 10); do node basicflow.test.js || exit 1; done"
  },
  "devDependencies": {
    "chromedriver": "128.x",
    "selenium-webdriver": "4.23.x"
  }
}
