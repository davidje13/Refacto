{
  "name": "@refacto/e2e",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "lint": "tsc --pretty && prettier --check .",
    "lint:tsc": "tsc --pretty",
    "lint:prettier": "prettier --check .",
    "test": "lean-test --preprocess tsc"
  },
  "config": {
    "detect_chromedriver_version": true
  },
  "devDependencies": {
    "@types/node": "20.x",
    "@types/selenium-webdriver": "4.x",
    "chromedriver": "147.x",
    "geckodriver": "6.1.x",
    "selenium-webdriver": "4.41.x"
  }
}
